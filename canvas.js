"use strict";

const DISPLAY_WIDTH = 1280;
const DISPLAY_HEIGHT = 1024;

let model;
let view;
let controller;

function setup() {
    createCanvas(DISPLAY_WIDTH, DISPLAY_HEIGHT);
}

function draw() {}